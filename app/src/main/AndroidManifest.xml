<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProgressioMobileApp"
        tools:targetApi="31">
        <activity
            android:name=".AnalyticsActivity"
            android:exported="false" />
        <activity
            android:name=".HomepageAdminActivity"
            android:exported="false"
            android:label="@string/title_activity_homepage_admin"
            android:theme="@style/Theme.ProgressioMobileApp" />
        <activity
            android:name=".HomepageUserActivity"
            android:exported="false"
            android:label="@string/title_activity_homepage_user"
            android:theme="@style/Theme.ProgressioMobileApp" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false"
            android:label="@string/title_activity_forgot_password"
            android:theme="@style/Theme.ProgressioMobileApp" />
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/Theme.ProgressioMobileApp" />
        <activity
            android:name=".SignInActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ProgressioMobileApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NotificationActivity">

            <!-- Add other activity configurations here if needed -->
        </activity>
        <activity android:name=".TaskDetailActivity" />
    </application>

</manifest>